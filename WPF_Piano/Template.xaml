<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WPF_Piano">

    <ControlTemplate x:Key="WhitePianoButton" TargetType="{x:Type local:CustomPianoButton}">
        <Border  BorderBrush="Black" BorderThickness="1" CornerRadius="4">
            <Button Background="White" Name="Background" >
                <!-- The Grid now has its Height and Width removed -->
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  MinWidth="28">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="{TemplateBinding KeyLabel}"
                   FontSize="16" FontWeight="Bold"
                   Margin="0,15,0,0"
                   Foreground="Black"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Grid.Row="0"/>
                    <TextBlock Text="{TemplateBinding NoteLabel}"
                   FontSize="14"
                   Margin="0,0,0,15"
                   Foreground="Black"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Grid.Row="2"/>
                </Grid>
            </Button>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="BlackPianoButton" TargetType="{x:Type local:CustomPianoButton}" >
        <Border Background="Black" BorderBrush="Black" BorderThickness="1" CornerRadius="4">
            <!-- The Grid now has its Height and Width removed -->
            <Button Background="Black" Name="Background">
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="{TemplateBinding KeyLabel}"
                 FontSize="16" FontWeight="Bold"
                 Margin="0,15,0,0"
                 Foreground="White"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Grid.Row="0"/>
                    <TextBlock Text="{TemplateBinding NoteLabel}"
                 FontSize="14"
                 Margin="0,0,0,15"
                 Foreground="White"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Grid.Row="2"/>
                </Grid>
            </Button>
        </Border>
    </ControlTemplate>
    <ControlTemplate x:Key="NoteTile" TargetType="{x:Type Control}">
        <Border BorderBrush="Black" BorderThickness="1" CornerRadius="4">
            <Border.RenderTransform>
                <TranslateTransform x:Name="FallTransform"/>
            </Border.RenderTransform>
            <Rectangle x:Name="NoteTile" Fill="LightBlue" />
        </Border>
        <ControlTemplate.Triggers>
          
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <DataTemplate x:Key="SongItemTemplate">
        <Border x:Name="ItemBorder" Background="#1A1A1A" CornerRadius="8" Margin="0,3" Padding="7" BorderBrush="#252525" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="160"/>
                </Grid.ColumnDefinitions>

                <Border Background="#252525" CornerRadius="5" Width="40" Height="40">
                    <TextBlock Text="♪" Foreground="#00FFCC" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" />
                </Border>

                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="SemiBold" FontSize="12" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding Author}" Foreground="#666" FontSize="10" Margin="0,2,0,0" />
                </StackPanel>
            </Grid>
        </Border>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True">
                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#00FFCC"/>
                <Setter TargetName="ItemBorder" Property="Background" Value="#252525"/>
            </DataTrigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ItemBorder" Property="Background" Value="#222"/>
            </Trigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>