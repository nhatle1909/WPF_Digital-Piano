<Window x:Class="WPF_Piano.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Piano"
        mc:Ignorable="d"
        Title="Piano Studio" Height="900" Width="1600"

        
        FontFamily="Segoe UI">
    <Window.Resources>

        <DropShadowEffect x:Key="DropShadow" Color="Black" BlurRadius="8" ShadowDepth="3" Opacity="0.3"/>

        <Style x:Key="CompactListItemStyle" TargetType="ListViewItem">
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.15*" />
            <ColumnDefinition Width="0.85*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,20" x:Name="InstrumentSettings">

            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,20">
                <Button Click="Button_Click" Background="White" Width="40" Height="40"  BorderBrush="White" ToolTip="Browse midi file" Margin="0,0,20,0">
                    <Image Source="/Resources/find.png" Stretch="Fill"/>
                </Button>

                <Button Click="PlayTest" Width="40" Height="40" Background="White" BorderBrush="White" ToolTip="Play">
                    <Image Source="/Resources/play-button.png" Stretch="Fill"/>
                </Button>

            </StackPanel>

      
        </StackPanel>

        <Grid Grid.Column="1">
            <Viewbox Stretch="Fill" SnapsToDevicePixels="True">
                <Border BorderBrush="Black" BorderThickness="2" Width="2520" Height="1400" >
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="1240*"/>
                            <RowDefinition Height="160*"/>
                        </Grid.RowDefinitions>
                       
                        <ScrollViewer Grid.Row="1" Name="NoteFrame" 
                              VerticalScrollBarVisibility="Hidden"
                              HorizontalScrollBarVisibility="Hidden"
                              VerticalAlignment="Stretch">
                            <local:NoteControl x:Name="NoteControl" Width="2520" RenderOptions.EdgeMode="Aliased" />
                        </ScrollViewer>
                        <Grid Grid.Row="2" Background="Transparent" Height="160">
                            <Viewbox Stretch="Fill" >
                                <StackPanel x:Name="PianoButtonOctave" Orientation="Horizontal"/>
                            </Viewbox>
                        </Grid>
                    </Grid>
                </Border>
            </Viewbox>
        </Grid>
    </Grid>

</Window>